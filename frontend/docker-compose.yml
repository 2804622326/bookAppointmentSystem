# This file is deprecated. Please use the main docker-compose.yml in the root directory.
# Use: docker-compose -f ../docker-compose.yml up

version: '3'
services:
  web:
    build: .
    ports:
      - "3000:80"
